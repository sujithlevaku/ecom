$('#cartItem').html("My Cart (<%=current_user.cart_line_items.pluck(:quantity).inject(:+)%>)");

  //$.growl({ title: "Growl", message: "The kitten is awake!", url: "/kittens" });
  //$.growl.error({ message: "The kitten is attacking!" });
  $.growl.notice({ message: "Item added to Cart" });
  // $.growl.warning({ message: "The kitten is ugly!" });
  alertify.success('Thank You');

// alertify.alert("<%=current_user.username.capitalize%>", 'Item Added to Your Cart!', function(){ alertify.success('Ok'); });



